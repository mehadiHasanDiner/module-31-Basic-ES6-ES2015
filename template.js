const neWMultiLine = `First line of Text
second line of Text
third line of Text
fourth line of Text
`;
// console.log(neWMultiLine);

const a = 20;
const b = 34;
const num = [1, 2, 3, 4, 5];
const newSummary = `sum of ${a} and ${b} is ${a + b}`;
const newSummaryArray = `sum of ${a} and ${num.length} is ${a + b}`;

console.log(newSummaryArray);
